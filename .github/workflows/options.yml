name: test options

environment:
  SERVER: production

on:
  workflow_dispatch:
    inputs:
      environment:
        type: environment
        required: true
        
jobs:
  test:
    runs-on: macos-12
    steps:
      - echo ${{ inputs.environment }}
